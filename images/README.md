Coloque aqui a captura de tela do NotebookLM com o nome `notebooklm-print.png`.

Para adicionar a imagem ao repositório, arraste o arquivo para a pasta `images/` neste projeto ou use:

```bash
cp /caminho/para/screenshot.png images/notebooklm-print.png
```

O `README.md` principal referencia `images/notebooklm-print.png` para exibir a captura.